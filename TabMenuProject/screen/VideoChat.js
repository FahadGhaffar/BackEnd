
import React from 'react';

import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
} from 'react-native';
import AIcon from 'react-native-vector-icons/AntDesign';

const VideoChat = () => {

    return (
        <View><Text>VideoChat Screen</Text></View>
    );
}

export default VideoChat;